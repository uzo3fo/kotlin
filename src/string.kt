//The String class represents an array of char types. Strings are immutable which means the length and elements cannot be changed after their creation.
fun main(args:Array<String>){
    var str1 = " what a hell"
    var str2 = " hello"
    println(str1.count())
    println(str1.drop(0))
   // println(str1.)
}